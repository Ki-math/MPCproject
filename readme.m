% Please run next scprits for preparering.
f = fullfile(pwd,'util');
cd(f); 

% If you want to add the path temporally, the addpath command just runs. 
addpath(pwd);

% If you want to save the path permanent, the savepath command runs.
savepath;